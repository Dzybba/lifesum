<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cardview_dark_background"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingVertical="20dp"
    tools:parentTag="android.widget.LinearLayout">

    <TextView
        android:id="@+id/food_item_title"
        style="@style/HeadingH3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/line_drawable"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textColor="?colorOnPrimary"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginHorizontal="15dp"
        tools:text="Title" />

    <TextView
        android:id="@+id/food_item_calories_num"
        style="@style/HeadingH2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingTop="20dp"
        android:paddingBottom="10dp"
        android:textColor="?colorOnPrimary"
        tools:text="400" />

    <TextView
        android:id="@+id/food_item_calories_text"
        style="@style/ParagraphP1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/food_item_calories_per_serving"
        android:textColor="?colorOnPrimary"
        android:textStyle="italic" />
</merge>
